# CHANGELOG

本アプリケーションの変更履歴を記録します。

## [2026-08-27] v0.0.9 - GPIO設定画面でのテスト点灯（OK/NG出力）の物理ピン駆動およびLED表示の修正

### 修正 (Fixed)

- **GPIO設定画面での「テスト点灯」物理ピン駆動およびLED表示の修正**:
  - 設定画面の「GPIOピン設定」タブ右側にある判定出力（OK出力 / NG出力）の「テスト点灯」ボタンを押した際に、画面上のLED Canvasおよび実際の物理GPIOピン（シグナルタワー/パトライト/ブザー等）が連動してON/OFFトグル点灯するように修正しました。
  - NG出力テスト時にLED表示が緑色になっていた不整合を修正し、OK出力は緑（`COLOR_OK`）、NG出力は赤（`COLOR_NG`）で視覚的にわかりやすく点灯表示されるように改善しました。
  - 設定画面を閉じる際（保存またはキャンセル時）にテスト点灯ピンを自動で消灯（OFFリセット）する安全ガードを追加しました。

---

## [2026-08-25] v0.0.8 - 高解像度タイル分割推論・位置ズレ対策エッジマスク・空間クラスタ傷判定・設定ダイアログ例外修正

### 追加 (Added)

- **高解像度タイル分割推論**: 大型ワーク全域を撮影した高解像度画像をそのまま縮小せず、重なり付きの複数タイルに分割して推論し、アノマリーマップをシームレスにブレンド再合成する機能を新設。微小な傷が画像縮小によって消失する問題を解決しました。
- **搬送位置ズレ対策・エッジ誤検知防止マスク**: 搬送レーン上の上下左右位置ズレに対して、ワーク表面領域を自動抽出し外形輪郭境界を侵食した検査マスクを適用する機能を追加。ワーク外周境界のシフトによるエッジ擬似異常や誤検知を遮断しました。
- **空間クラスタリング・傷形状サイズ判定**: 最大異常値依存を脱却し、閾値以上の異常画素クラスタの面積および長さを自動算出・評価する判定ロジックを実装。点状の光反射ムラや微小ノイズを自動除外し、実欠陥のみを確実にNG判定します。
- **欠陥バウンディングボックス可視化**: 検出された傷・欠陥クラスタ領域の周囲に赤色ハイライト矩形とスコアを描画するオーバーレイ機能を統合。
- **微小欠陥検査パラメータ設定UI**: 詳細設定ダイアログに「微小欠陥検査 & 位置ズレ対策設定」カード（タイル推論、最小欠陥面積、最小欠陥長、エッジ除外幅）を新設。

### 修正 (Fixed)

- **設定ダイアログ起動時の例外修正**: `modules/dialogs.py` のトップレベルで `import sys` が欠落していたため、設定ダイアログのシステムツールタブ内でOS判定を行う際に発生していた `NameError` クラッシュを解消しました。

---

## [2026-08-18] v0.0.7 - ドアラインFr/Rr対応・スピンボックス暴走防止修正

### 追加 (Added)

- **ドアライン対応 (Fr/Rr 分割判定 & 0.5刻みコミット)**:
  - **コミット番号の Fr / Rr 表記化**: メイン画面UI、設定画面、テンキー入力、保存ファイル名、CSVログにおいて、従来の `.5` 表記を廃止し、整数時は **`5946 Fr`**（ファイル/CSV: `5946Fr`）、小数時は **`5946 Rr`**（ファイル/CSV: `5946Rr`）として統一しました。
  - **整数時 (Fr時) パターン決定 & 1台引き継ぎ**: ドアライン対応時、コミット番号が整数（Fr時）になった時のみパターンピン（または遅延キュー）からパターンを取得・固定し、続く小数（Rr時）はFr時と同一のパターンIDを引き継いで判定するように改善しました。
  - **パターン設定タブでの Fr / Rr 個別判定条件設定 (PatchCore対応)**: 各パターンのトリガー別ステージ設定に **[Fr 判定条件]** / **[Rr 判定条件]** の切り替えサブタブを新設。同一トリガー・カメラに対してFrとRrで別々のPatchCoreモデルファイル (.ckpt) やしきい値を設定・評価できるようにしました。

### 修正 (Fixed)

- **スピンボックス操作時の数値暴走・連続変化防止**: ラズベリーパイ（Linux / X11 / タッチパネル環境）において、スピンボックス（`Spinbox`）の矢印操作時にクリックやタッチを離しても内部リピートタイマーが停止せず数値が勝手に増減・暴走し続ける不具合を解消しました。`repeatdelay=0, repeatinterval=0` の設定およびマウスボタン解放（`<ButtonRelease-1>`）、フォーカス外れ（`<FocusOut>`）、マウス離脱（`<Leave>`）時の強制タイマー停止ハンドラを導入し、1クリックで確実に1ステップずつ安全に増減するように修正しました。
- **テンキーダイアログ (Fr/Rr 切替・非0.5刻み時の小数点削除)**: テンキー入力画面において、0.5刻みモードOFF時は小数点キーを非表示化して誤入力を防止し、ドアライン時は現在の状態（Fr/Rr）に応じてボタンテキストが動的に「Fr」「Rr」と切り替わるように改善しました。
- **操作パネルの「＋」ボタンはみ出し・消失防止**: コミット番号フォントサイズおよびレイアウト配置（`＋` ボタンの先行右寄せ pack）を最適化し、`0001 Fr` 表示時でもボタンが枠外に押し出されず確実に表示されるように修正しました。
- **パターン設定タブのトリガー別スコープ分離とシームレス切り替え (チラつき・レイアウト崩れ解消)**: 各トリガーカード内にFr用・Rr用のフレームを事前保持して `pack`/`pack_forget` で瞬時スワップする方式を採用し、完全なシームレス切り替えとスクロール位置保持を実現しました。

---

## [2026-08-08] v0.0.6 - カメラ探索非同期化および非カメラノード(bcm2835-codec等)の除外フィルター強化

### 変更 (Changed)

- **設定画面表示の非同期化 (0秒パッと開くUI)**: 設定画面およびカメラ設定タブを開く際、カメラ探索を別スレッド (`threading.Thread`) でバックグラウンド非同期実行するように改修。設定ボタンを押した瞬間に画面がフリーズすることなく一瞬で開くように改善しました。

### 修正 (Fixed)

- **非カメラノード (bcm2835-codec 等) の即時除外と警告・タイムアウト排除**: Raspberry Pi の `/dev/video19` ～ `/dev/video21` 等の動画デコーダ/エンコーダ/ISPノード (`bcm2835-codec`, `bcm2835-isp`, `rpivid` 等) に対する無差別な `cv2.VideoCapture` を即座にスキップ・除外する判定フィルターを導入。キャプチャオープン時の長時間のタイムアウトおよび警告ログ (`can't open camera by index`) 多発・UIフリーズを完全解決しました。
- **OpenCV 警告ログの非表示化**: `cv2.utils.logging.LOG_LEVEL_SILENT` を適用し、カメラ非接続時の無用なコンソール警告出力を消音化しました。

---

## [2026-08-05] v0.0.5 - 接続カメラデバイス名自動取得およびプルダウン選択機能の統合

### 追加 (Added)

- **認識カメラデバイス名の自動探索機能 (`detect_available_cameras`)**: Linux (/sys/class/video4linux) および Windows (PowerShell Win32_PnPEntity / DirectShow) の両対応で、OSが認識している実際のカメラデバイス名（例: `Integrated Camera`, `USB Video Device` など）を全自動検出する関数を実装しました。

### 変更 (Changed)

- **カメラ設定UIのプルダウン化**: 「詳細設定」>「カメラ」タブにおけるインデックスの数値指定（スピンボックス）を排除し、検出されたカメラデバイス名一覧を直感的に選択できるプルダウンメニュー (`ttk.Combobox`) に一括変更しました。

---

## [2026-08-04] v0.0.4 - PaDiM異常検知モデル対応・日時設定・USBスピーカー復旧・システムUI再編

### 追加 (Added)

- **PaDiM (Process for Anomaly Detection) アルゴリズムの同時サポート**: PatchCore モデルに加え、PaDiM アルゴリズムで生成された `.ckpt` モデルファイルを自動検出・相互ロードして推論を行う機能を追加しました。
- **ラズパイ本体日時設定機能 (`SystemDateTimeDialog`)**: 「詳細設定」>「システム」設定タブに「ラズパイ本体の日時を設定」ボタンを追加。年月日・時分秒の入力および端末現在時刻セットにより、Linux/Raspberry Pi のシステムクロック（`timedatectl` / `date` / `hwclock`）を安全に更新できる設定ダイアログを実装しました。
- **USBスピーカー自動出力設定・復旧機能**: 「USBスピーカー音声出力を自動復旧・設定」ボタンを追加。音が出ない場合に ALSA / PulseAudio / PipeWire の設定自動修正、ミュート解除、音量100%化、Pygame Mixer 再初期化を一括実行するレスキュー機能を統合しました。

### 変更 (Changed)

- **システム設定タブの5カテゴリカード整理統合**: 設定項目を `AI・推論パラメータ設定`, `出力制御 & 生産ライン同期`, `ファイルパス & 容量自動管理`, `音声・ブザー設定`, `システムツール & メンテナンス` の5つの領域に再編し、視認性・操作性を向上させました。
- **UI項目名・操作ガイドの平易化**: （）付きの英語・技術用語表記や冗長なカッコ書きを排除し、平易で分かりやすい日本語表現に全画面で統一。

---

## [2026-07-31] v0.0.3 - デスクトップ起動スクリプト自動作成機能

### 追加 (Added)

- **システム設定タブへ起動ショートカット作成ボタンを追加**: 「詳細設定」>「システム」設定タブから、デスクトップ上に本システムをワンクリックで起動できるスクリプトファイル（Windows用 `.bat` および Linux/macOS用 `.sh`）を自動生成する機能を追加しました。

---

## [2026-07-28] v0.0.2 - 撮影モード保存先のフォルダ分け

### 変更 (Changed)

- **撮影モード(REC)画像のフォルダ分け保存**: 撮影モードで取得した画像の保存先を、パターン・カメラ・トリガーの組み合わせごとのサブフォルダに整理するように変更しました。  
  変更前: `results/images/REC/{ファイル名}.jpg`  
  変更後: `results/images/REC/{パターン名}_{カメラ名}_{トリガー名}/{ファイル名}.jpg`  
  例: `REC_9163_J12S ｱﾝﾀﾞｰｶﾊﾞｰ　ﾍﾞﾛ有_カメラRH_トリガー 1_02.jpg` は  
  `results/images/REC/J12S ｱﾝﾀﾞｰｶﾊﾞｰ　ﾍﾞﾛ有_カメラRH_トリガー 1/` 配下に保存されます。  
  検査モード（OK / NG / SKIP 等）の保存先は従来通りです。

---

## [2026-07-26] v0.0.1 - AI自動検査システム (PatchCore版) 初版

### 追加 (Added)

- **PatchCore 異常検知判定アルゴリズムの実装**: PyTorch / PatchCore による正常特徴メモリバンクの読み込み、特徴抽出、および異常スコア計算・しきい値判定機能を実装しました。
- **マルチカメラ ＆ トリガー連動自動検査機能**: 複数カメラ並列キャプチャ、GPIO外部トリガー検出、セレクターピンによる検査パターン切り替え機能を備えた全自動検査制御を構築しました。
- **未選択ckptモデルの自動SKIP（検査対象外）化**: パターン設定で `.ckpt` モデルファイルが未選択（空欄）またはファイルが存在しないカメラ・パターンについて、検査を実施せず自動的に `SKIP`（非対象）として処理するロジックを統合しました。
- **全主要UIコントロールへのヘルプツールチップ追加**: メイン画面および詳細設定画面（システム・カメラ・GPIO・パターン・画素数設定）の各ボタン、入力フィールド、スライダー、参照ボタンに `Tooltip` を付与し、マウスオーバーで詳細説明が表示されるように改善しました。
- **証拠画像保存 ＆ 日別CSV集計**: 判定結果ごとの画像（OK, NG, NG_RAW, SKIP, REC）を自動保存し、検査結果ログをCSVファイルに追記出力する機能を実装しました。
- **Windows モックモード対応**: GPIO非搭載のPC環境で動作可能な仮想GPIOダイアログおよびカメラモック機能を標準統合しました。

### 変更 (Changed)

- **バージョン体系およびドキュメントの一新**: 本リポジトリ（`inspection_app_patchcore`）のバージョン体系を `v0.0.1` として再定義し、`README.md` および `CHANGELOG.md` をPatchCore異常検知仕様に即して更新・一新しました。

### 修正 (Fixed)

- **Windows環境における日本語画像ファイル名文字化けの修正**: OpenCV の `cv2.imwrite` が Windows ANSI コーディング（CP932）経由でパス解釈を行うことによる日本語ファイル名文字化け（`繝代ち繝ｼ繝ｳ` 等）を、`cv2.imencode` + Python `open(path, "wb")` による書き込み方式へ変更することで修復しました。
- **設定ダイアログの `ckpt` 参照ボタン動作不具合の修正**: パターン設定タブのモデル選択欄にある「参照」ボタン (`btn_browse`) に `command=_pick_model` を設定し、ボタン押下でファイル選択ダイアログが正しく起動するように修復しました。
- **設定ダイアログ起動エラー (`NameError`) の修正**: メインアプリケーション (`modules/app.py`) において `SettingsDialog` のインポート文が不足していた問題を修正しました。
- **`modules/dialogs.py` 内の構文エラー (`SyntaxError`) の修正**: `modules/dialogs.py` のファイル先頭に存在していた不要なコードブロック記法 (```python) を除去し、正常にモジュールをインポート・読み込みできるように修復しました。

---
